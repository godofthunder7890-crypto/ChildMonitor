# This workflow is intentionally disabled — see build.yml for the active release build
# Kept as a file to avoid accidental recreation; trigger removed to prevent duplicate runs
name: Build APK (disabled)
on:
  workflow_dispatch:
    inputs:
      dummy:
        description: "disabled"
        required: false
jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo disabled

